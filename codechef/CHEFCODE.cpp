#include <bits/stdc++.h>

using namespace std;

void generate(){

	generate();
	
	
}

int main(){

	int n,k;
	cin >> n >> k;

	int a[n];
	for(int i=0;i<n;i++)
		cin >> a[i];

	generate();
	
	return 0;
}