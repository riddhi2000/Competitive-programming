#include <bits/stdc++.h>
using namespace std;

void solve(){
	int n,q,a,b,c,d;
	cin >> n >> q;
	int arr[n];

	for(int i=0;i<n;i++)
		cin >> arr[i];

	for(int i=0;i<q;i++){
		cin >> a >> b >> c >> d;

		
	}
}

int main(){

	int t;
	cin >> t;

	while(t--)
		solve();

	return 0;
}